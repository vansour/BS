# Thesis Citation Audit

说明：本清单列出当前编译主线章节中未显式包含引用命令的正文段落。
其中摘要、方法自述、实验结果解读、结论与展望中的作者原创总结，允许合理无引文。

## 00_abstract_cn.tex

- 未含引用段落数：`4`

1. 行号约 `1`；分类：摘要通常不加引文
   片段：高速公路作为国家综合交通运输体系中的关键基础设施，具有通行速度高、交通流量大、运行场景连续性强等特点。在高速公路运营过程中，团雾、平流雾、辐射雾等低能见度天气会显著削弱驾驶员的前方可视距离，导致目标识别延迟、制动决策滞后和追尾连撞风险上升。与普通城区雾天不同，高速公路团雾往往具有局地突发、空间不均匀、持续时间短但危害强的特点，因此仅依赖固定阈值管控或人工视频
2. 行号约 `3`；分类：摘要通常不加引文
   片段：本文首先从真实工程约束出发，针对团雾样本难获取、雾浓度标签难标注、现场部署算力有限等问题，提出一种“晴天交通视频数据复用 + 单目深度估计 + 在线物理造雾 + 统一多任务训练”的技术路线。具体而言，本文以 UA-DETRAC 交通监控数据为基础数据源，通过序列级划分构建训练集与验证集；利用 MiDaS\_small 对原始清晰图像进行深度预计算并形成缓存，
3. 行号约 `5`；分类：摘要通常不加引文
   片段：在系统实现层面，本文进一步完成了训练、推理、可视化和部署导出闭环。训练脚本支持 checkpoint 续训、缺失深度缓存自动补算、AMP 混合精度训练、非有限梯度监测以及 QAT/INT8 转换路径；推理系统在视频帧上执行 letterbox 预处理、三任务联合前向、beta 指数滑动平均与动态置信度调节，并将车辆检测结果与雾状态联合绘制到输出视频中。针对真
4. 行号约 `7`；分类：摘要通常不加引文
   片段：从研究意义看，本文工作的价值主要体现在三个方面：第一，在数据层面，给出了一种不依赖大规模真实团雾标注样本、能够复用现有晴天交通视频的数据构造方法；第二，在模型层面，探索了高速公路团雾监测中检测、天气识别与雾浓度估计的统一多任务建模思路；第三，在工程层面，形成了一条从数据预处理、模型训练到视频推理和部署导出的完整实现链路，可为后续开展团雾预警、可变限速联动、交

## 01_abstract_en.tex

- 未含引用段落数：`4`

1. 行号约 `1`；分类：摘要通常不加引文
   片段：Expressways are critical infrastructures in modern transportation systems, where traffic flow is dense, vehicle speed is high, and safety margins are relatively small. In such scen
2. 行号约 `3`；分类：摘要通常不加引文
   片段：The proposed method starts from practical constraints in engineering deployment. Since large-scale real foggy traffic datasets with dense labels are difficult to acquire, this work
3. 行号约 `5`；分类：摘要通常不加引文
   片段：On top of this data pipeline, a unified multi-task model is designed. The model uses a YOLO detector as the shared backbone and neck, while two lightweight heads are attached to hi
4. 行号约 `7`；分类：摘要通常不加引文
   片段：Beyond the basic inference chain, the current system also introduces a trajectory-level temporal filtering module to suppress persistent static false positives that visually resemb

## 02_chapter_01.tex

- 未含引用段落数：`18`

1. 行号约 `13`；分类：待人工判断
   片段：因此，围绕固定监控视频中的团雾监测构建一个兼顾检测、天气识别与雾浓度估计的统一系统，既具有明显的理论研究意义，也具有较强的实际应用价值。对于管理部门而言，这样的系统可以作为交通气象监测体系的视觉补充；对于道路运营方而言，它有助于实现对高风险雾区路段的精细感知；对于学术研究而言，它则提供了一个将目标检测、低层退化建模、单目深度估计和多任务学习有机结合的问题场景
2. 行号约 `39`；分类：待人工判断
   片段：总体来看，国外研究在恢复模型、恶劣天气检测与参数估计方面发展迅速，但仍存在几个不足：第一，不少方法依赖自动驾驶前视相机数据，与固定监控视频存在视角差异；第二，大量工作集中于恢复后检测而非直接面向交通业务指标；第三，许多方法仅输出检测结果而缺乏可解释的雾强度量化；第四，面向固定交通监控系统的统一多任务建模仍相对不足。本文的工作正是在这一背景下，尝试将检测、天气
3. 行号约 `57`；分类：待人工判断
   片段：尽管如此，国内研究仍存在一些短板。其一，公开、规范、可复现的高速公路团雾视频数据集仍然不足，导致许多方法难以横向对比；其二，很多方法关注去雾质量，却没有把雾分类、浓度估计和目标检测整合为统一输出；其三，部分模型面向论文指标设计，工程链路不完整，难以直接迁移至监控中心现有视频系统；其四，真实业务更需要“雾状态 + 车辆状态 + 风险提示”的组合输出，而非单一去
4. 行号约 `61`；分类：待人工判断
   片段：综合国内外研究现状，可以发现现有工作虽然已经取得大量成果，但距离“固定交通监控视频中的可部署团雾监测系统”仍有明显差距。
5. 行号约 `67`；分类：待人工判断
   片段：第三，工程化程度不足。许多论文方法没有公开完整代码，或者只给出模型结构而缺乏训练、验证、推理、导出与部署闭环，不利于毕业设计和工程实践验证。
6. 行号约 `73`；分类：待人工判断
   片段：针对高速公路团雾监测，本文关注的核心问题可以概括为：如何在固定道路监控视频中，利用可获取的清晰天气交通数据构建可训练样本，并设计一个能够同时输出车辆检测、雾类型分类和雾浓度估计结果的统一模型，从而形成可部署的视频监测原型系统。
7. 行号约 `75`；分类：待人工判断
   片段：围绕上述问题，实际研究面临以下几类关键挑战。
8. 行号约 `79`；分类：待人工判断
   片段：第二，雾天气具有明显的空间非均匀性。许多传统去雾或雾检测研究将整幅图像视作均匀退化过程，而高速公路团雾往往具有局部增强、片状分布和随深度变化的非均匀特征。若模型只基于全局平均亮度或简单手工特征，很难稳定地区分无雾、均匀雾和局地团雾。
9. 行号约 `85`；分类：待人工判断
   片段：第五，工程部署要求算法链路完整且具备可解释性。单一的分类结果难以满足道路管控需求；单一的目标检测结果又无法说明天气风险；单一的去雾结果虽改善视觉观感，但并不天然等价于风险量化。因此，实际系统更需要“可见结果 + 可解释指标 + 可扩展部署路径”的综合解决方案。
10. 行号约 `89`；分类：待人工判断
   片段：围绕上述问题，本文设定以下研究目标：
11. 行号约 `98`；分类：待人工判断
   片段：为达成以上目标，本文主要工作包括：
12. 行号约 `111`；分类：待人工判断
   片段：结合项目目标与当前实现，本文的创新点主要体现在以下几个方面。
13. 行号约 `113`；分类：待人工判断
   片段：第一，提出了一种面向固定交通监控视频的“深度缓存 + 在线物理造雾”数据构建策略。该方法不需要预先生成海量雾图，也不强依赖大规模真实雾天气标注，而是借助深度估计和大气散射模型，在训练阶段动态生成不同类型和强度的雾样本，从而降低数据获取成本并提升样本多样性。
14. 行号约 `115`；分类：待人工判断
   片段：第二，设计了一种面向高速公路团雾监测的统一多任务模型。与传统仅进行车辆检测或仅进行天气分类的方法不同，本文将车辆检测、雾类型分类和雾浓度回归整合到同一网络中，以共享高层语义特征实现三任务协同学习，增强了模型的整体表达能力和系统一体化程度。
15. 行号约 `117`；分类：待人工判断
   片段：第三，从工程可落地角度提出了“统一模型主链 + 混合推理备份链”的系统实现方案。当统一模型在训练早期对车辆检测的鲁棒性尚未充分提升时，可通过 YOLO11n 检测器与雾估计头组合形成混合推理路径，保证系统演示与业务验证不断链。这种设计体现了从纯学术模型走向工程系统时对稳定性与可交付性的综合考虑。
16. 行号约 `119`；分类：待人工判断
   片段：第四，建立了较为完整的训练与部署工具链。本文不仅完成了网络结构设计，还实现了数据审计、深度缓存预计算、训练前 smoke test、视频推理、ONNX 导出以及 QAT/INT8 路径，为后续边缘部署和性能优化留下了明确扩展接口。
17. 行号约 `123`；分类：待人工判断
   片段：为与本科毕业设计论文范文的章节组织方式保持一致，同时保留本文当前已经形成的工程深度与实验内容，全文结构重组为八章。
18. 行号约 `125`；分类：待人工判断
   片段：第一章为绪论，主要介绍研究背景、陕西交通运输应用场景、国内外研究现状、研究问题与技术挑战、研究目标与主要内容以及本文创新点。第二章介绍大气散射模型、单目深度估计、多任务学习和目标检测等理论基础与关键技术。第三章从工程视角给出系统需求分析、总体技术路线、模块划分和应用场景分析。第四章详细说明数据集构建、深度缓存管理、多任务数据组织以及在线团雾合成方法。第五章重

## 04_chapter_03.tex

- 未含引用段落数：`7`

1. 行号约 `15`；分类：待人工判断
   片段：其中，\(I(x)\) 表示观测到的退化图像，\(J(x)\) 表示场景真实辐射亮度，\(A\) 表示大气光，\(t(x)\) 为透射率。若进一步考虑场景深度 \(d(x)\) 与散射系数 \(\beta\)，则有：
2. 行号约 `23`；分类：待人工判断
   片段：需要指出的是，真实高速公路团雾并不一定完全满足理想的均匀散射假设。团雾常常表现为局地浓淡不均、横向分布变化强、边界模糊等特点。因此，若仅按全局均匀透射率进行模拟，合成结果会缺乏足够的空间变化。为解决这一问题，本文在 \texttt{patchy} 模式中进一步引入低频噪声，使有效深度在空间上发生调制，从而得到更符合团雾局地变化特征的合成样本。
3. 行号约 `35`；分类：待人工判断
   片段：本文采用“共享 YOLO 检测主干 + 分类头 + 回归头”的结构，三项任务联合优化，其总损失可写为：
4. 行号约 `41`；分类：待人工判断
   片段：其中，\(L_{det}\) 为目标检测损失，\(L_{fog\_cls}\) 为雾分类交叉熵损失，\(L_{fog\_reg}\) 为 beta 回归均方误差损失。当前实现中三项权重均设置为 1.0，后续也可以根据任务收敛速度和梯度尺度进行进一步平衡。
5. 行号约 `51`；分类：待人工判断
   片段：在视频监控场景中，雾浓度不应当被视为完全独立的逐帧噪声，而更应当具有一定时间连续性。为了避免单帧 beta 回归波动过大影响显示与下游决策，本文在推理阶段采用指数滑动平均（EMA）进行平滑：
6. 行号约 `57`；分类：待人工判断
   片段：其中 \(\alpha\) 为平滑系数。当前实现中，\texttt{EMA\_ALPHA = 0.1}。随后系统根据平滑后的 beta 值动态调整车辆显示阈值，使系统在低能见度场景下保留更多潜在目标，从而增强监测可见性。这体现了本文方法的一项重要特征：环境感知结果并非孤立输出，而是反过来影响检测展示策略。
7. 行号约 `61`；分类：待人工判断
   片段：本章围绕本文方法所依赖的关键理论基础进行了梳理。首先，大气散射模型为在线造雾和 beta 回归提供了物理启发；其次，单目深度估计为在无双目和无激光雷达条件下恢复场景远近关系提供了可行手段；再次，多任务学习解释了为何车辆检测、雾分类和雾浓度回归能够在同一主干中协同建模；随后，YOLO 骨干为统一模型提供了成熟的多尺度目标表达能力；最后，时间平滑与动态阈值机制则

## 05_chapter_04.tex

- 未含引用段落数：`19`

1. 行号约 `5`；分类：系统设计/实现自述，可合理无引文
   片段：在明确研究背景、文献现状和理论基础之后，论文需要进一步回答“系统究竟要解决什么问题、整体应如何组织、模块之间如何协同”这类工程层面的核心问题。对本科毕业设计而言，系统总体设计并不是可有可无的过渡内容，而是连接“研究动机”和“代码实现”的关键桥梁。本章将从系统需求分析出发，说明本文为何选择统一模型作为主路线、为何保留混合推理作为工程备份链、以及各功能模块如何围
2. 行号约 `9`；分类：系统设计/实现自述，可合理无引文
   片段：结合高速公路团雾监测的业务需求与当前项目实现条件，本文将系统需求归纳为以下四个层面。
3. 行号约 `13`；分类：系统设计/实现自述，可合理无引文
   片段：在数据层面，系统需要在不具备大规模真实团雾样本的前提下完成训练，这要求数据构建方式具备可复用、可扩展、可持续的特点。换言之，不能将系统建立在“必须不断人工采集真实团雾视频并逐帧标注”的高成本前提上。
4. 行号约 `15`；分类：系统设计/实现自述，可合理无引文
   片段：在算法层面，系统需要尽量减少模型数量与部署链路复杂度。若车辆检测、天气识别和雾浓度估计分别由三个独立模型承担，则推理链会更长、维护成本更高，也不利于部署和答辩展示。因此，统一模型是更合理的总体方向。
5. 行号约 `17`；分类：系统设计/实现自述，可合理无引文
   片段：在工程层面，系统不仅要能训练，还必须能推理、能导出、能可视化。毕业设计不仅关注模型思想是否成立，还关注完整工程链路是否打通。为此，数据审计、深度预计算、训练脚本、推理脚本、混合推理、视频导出、QAT 与 ONNX 导出都应当纳入总体设计。
6. 行号约 `21`；分类：系统设计/实现自述，可合理无引文
   片段：本文系统总体技术路线可概括为：
7. 行号约 `35`；分类：系统设计/实现自述，可合理无引文
   片段：这一技术路线兼顾了物理启发、深度学习表示能力和工程部署要求，避免了完全依赖手工特征或完全依赖难获取真实雾数据的极端方案。
8. 行号约 `46`；分类：系统设计/实现自述，可合理无引文
   片段：从代码实现角度看，本文系统可划分为以下几个核心模块。
9. 行号约 `60`；分类：系统设计/实现自述，可合理无引文
   片段：本文系统具有以下几个鲜明特点。
10. 行号约 `62`；分类：系统设计/实现自述，可合理无引文
   片段：其一，真实配置源统一。与许多“文档配置和代码配置并存、易造成版本漂移”的项目不同，本文明确以 \texttt{src/config.py} 作为真实配置入口，使训练、推理和导出均围绕统一参数展开。
11. 行号约 `64`；分类：系统设计/实现自述，可合理无引文
   片段：其二，训练与推理几何空间保持一致。无论是数据集侧的 letterbox 处理，还是推理时的视频帧预处理，本文均采用一致的几何对齐方式，从而降低训练-推理几何失配带来的性能波动。
12. 行号约 `66`；分类：系统设计/实现自述，可合理无引文
   片段：其三，系统从一开始就考虑了部署和容错。例如，训练脚本支持缺失深度缓存自动补算；推理系统支持权重自动回退；导出脚本支持 ONNX 输出；量化路径保留了 QAT 与 INT8 转换能力。这使得本项目不仅是一个研究方法，也是一套具备工程闭环特征的原型系统。
13. 行号约 `68`；分类：系统设计/实现自述，可合理无引文
   片段：其四，系统已初步具备视频级误检治理能力。当前版本不再停留在“每帧独立检测”的层面，而是通过轨迹级时序过滤和 patch 级二次复核，对真实监控视频中的静止假车误检进行抑制，并通过日志、patch 和 hard negative 数据回灌形成闭环。
14. 行号约 `70`；分类：系统设计/实现自述，可合理无引文
   片段：其五，系统具有较强的扩展性。后续若要引入真实雾视频监督、加入可见度等级估计、融合更多交通气象传感器数据，或将二次复核器替换为更大规模的专用分类器，均可在当前框架上继续演进，而无需完全推翻已有实现。
15. 行号约 `76`；分类：系统设计/实现自述，可合理无引文
   片段：从需求结构看，本文系统与陕西交通部门现状的结合点可归纳为表 4-1。
16. 行号约 `96`；分类：系统设计/实现自述，可合理无引文
   片段：从实际部署顺序看，陕西场景更适合采用“先视频增量、后系统融合”的渐进式策略，而不是一次性替换既有监测体系。第一阶段可优先在交通量大、监控条件成熟的西安绕城、新机场线、连霍高速关中段等线路开展验证，重点考察视频流接入、推理稳定性、结果解释性和指挥调度可接受性；第二阶段可向桥隧比高、天气变化快、跨省联动需求强的陕南山区通道拓展，特别是岚皋—陕渝界、大巴山隧道周边
17. 行号约 `98`；分类：系统设计/实现自述，可合理无引文
   片段：从系统接口角度看，本文项目也较符合陕西现有信息化体系的接入逻辑。当前陕西交通系统已具备路网周报、监测预警建设、出行服务平台和应急转发机制，因此本文项目完全可以定位为“算法侧插件”而不是“平台侧重构工程”：输入端接入既有 CCTV 或监控视频流，算法端输出雾类型、beta、检测结果和风险级别，平台端再决定是否触发限速诱导、情报板提示、值班提醒或会商研判。这种松
18. 行号约 `100`；分类：系统设计/实现自述，可合理无引文
   片段：最后，需要强调的是，陕西交通部门当前推进的监测预警工程更多偏向结构安全、灾害点监测和综合感知，而本文项目聚焦于“低能见度视频智能感知”。两者并非竞争替代关系，而是互补关系：前者更关注基础设施与路网安全状态，后者更关注固定视频中车辆可见性、天气状态和通行风险的实时表达。若未来在陕西场景中开展进一步应用验证，本文项目最合理的定位应当是既有监测预警体系中的“视频雾
19. 行号约 `104`；分类：系统设计/实现自述，可合理无引文
   片段：本章从系统需求、技术路线、模块划分、系统特点以及陕西交通部门应用场景五个层面对本文原型系统进行了总体设计说明。通过本章分析可以看出，本文并不是单纯提出一个雾识别模型，而是围绕高速公路固定监控视频构建了一条从数据准备、模型训练到视频推理、导出部署和误检治理的完整系统链路。该总体设计为后续数据集构建、在线团雾合成、统一多任务模型设计以及实验验证奠定了工程框架。

## 06_chapter_05.tex

- 未含引用段落数：`15`

1. 行号约 `11`；分类：待人工判断
   片段：根据当前工作区运行时统计结果，原始训练数据目录中共包含 60 个监控序列，测试目录中包含 40 个监控序列。按照当前代码中的随机种子、帧步长和序列级划分策略，运行时实际得到训练集 48 个序列、67790 个样本，验证集 12 个序列、16001 个样本。进一步统计可知，当前训练集包含 66811 个有标注样本、473719 个检测框，验证集包含 15274
2. 行号约 `13`；分类：待人工判断
   片段：这组数据规模对于毕业设计和工程原型而言已经具备较高的可用性。一方面，车辆目标数量足够支撑单类检测训练；另一方面，固定监控视频序列的数据组织形式，十分适合研究“相同机位、不同气象退化下的联合感知问题”。然而，该数据集也存在局限：它缺少真实团雾标签，无法直接监督雾分类与浓度回归；此外，由于原始标注只覆盖车辆目标，因此天气任务必须通过合成方式构建监督信号。
3. 行号约 `26`；分类：待人工判断
   片段：这种划分方式有两个好处。其一，它保证了训练集与验证集之间不存在相邻帧泄漏，更符合视频任务的真实评估要求。其二，它使深度缓存预计算、数据审计脚本和训练主流程共享同一划分规则，从而避免“训练划分一套，预计算划分另一套”的工程混乱。当前系统中，\texttt{Config.TRAIN\_RATIO = 0.8}，随机种子为 42，因而每次重新构建数据集时都可得到稳
4. 行号约 `32`；分类：待人工判断
   片段：具体实现为：首先调用 \texttt{DepthEstimator} 使用 MiDaS\_small 对每张图像估计相对深度，随后将深度图插值回原图尺寸、归一化到 \texttt{[0,1]} 区间，并统一处理为“0 表示近、1 表示远”的深度约定，再以 \texttt{.npy} 格式保存到 \texttt{outputs/Depth\_Cache/}。缓
5. 行号约 `34`；分类：待人工判断
   片段：从当前运行情况来看，深度缓存覆盖已经完整。工作区中的深度缓存目录为 \code{outputs/Depth_Cache}，其中 \code{.npy} 文件数量与原始训练图像总量一致。训练前 smoke test 也确认在抽查范围内训练集和验证集未发现缺失缓存。训练脚本还进一步实现了自动补算策略：即使用户没有手动执行深度预计算，只要发现 train/val 
6. 行号约 `40`；分类：待人工判断
   片段：在具体实现中，数据集完成了以下几项关键工作：
7. 行号约 `50`；分类：待人工判断
   片段：值得强调的是，本文的输入空间处理保持了较强的一致性。无论是训练还是推理，系统都优先使用 letterbox，而不是简单拉伸。这是因为固定监控画面中横纵比往往固定，若直接拉伸至正方形，将改变车辆形状与车道透视比例，影响检测与天气判别的一致性。
8. 行号约 `56`；分类：待人工判断
   片段：在线造雾共构造三类天气样本：
9. 行号约 `64`；分类：待人工判断
   片段：在参数采样方面，系统根据 \texttt{Config} 中设置的物理范围对 beta 与大气光 A 进行随机采样，其中 \texttt{BETA\_MIN = 0.02}、\texttt{BETA\_MAX = 0.1}、\texttt{A\_MIN = 0.7}、\texttt{A\_MAX = 0.95}。对于 \texttt{uniform} 样本，
10. 行号约 `66`；分类：待人工判断
   片段：从任务监督角度看，这一模块具有双重作用。首先，它提供雾类别标签，支撑分类头训练；其次，它输出实际用于生成雾图的 beta 值，支撑回归头学习。这意味着雾分类与 beta 回归不再依赖人工标注，而是由在线合成过程自动生成监督信号。虽然这种监督属于“合成标签”，与真实物理能见度并不严格等价，但它为模型学习“相对雾强度”提供了稳定、连续且可控的训练信号。
11. 行号约 `70`；分类：待人工判断
   片段：为何本文不直接收集雾天视频进行监督，而选择清晰数据加在线造雾？原因主要有三点。
12. 行号约 `72`；分类：待人工判断
   片段：首先，真实团雾样本获取成本高且分布稀疏。团雾往往发生在特定路段、特定季节和特定时段，不易形成稳定的大规模数据集。即使采集到视频，如何给每一帧准确赋予可比的浓度标签也是困难问题。
13. 行号约 `74`；分类：待人工判断
   片段：其次，在线造雾虽然不能完全还原真实世界，但能够用统一的物理启发生成可控监督，并通过大规模多样化组合提升模型学习到的退化规律。对于毕业设计与工程原型阶段，这是一种成本与效果之间较为合理的折中。
14. 行号约 `76`；分类：待人工判断
   片段：再次，本文关注的是“监测系统原型”而非高精度气象物理反演器。因此，beta 在本文中首先被视作“与雾强度正相关、可回归、可解释、可平滑”的相对指标，而不是需要绝对匹配气象站能见度的严格物理量。只要该指标能够稳定反映从清晰到严重退化的变化趋势，并能与视频检测和风险显示联动，就具备了实际价值。
15. 行号约 `87`；分类：待人工判断
   片段：本章围绕“训练数据如何构造”这一核心问题，对本文的数据组织路线进行了详细说明。首先，UA-DETRAC 为本文提供了与固定交通监控视角较为匹配的清晰天气基础数据；其次，序列级 train/val 划分避免了视频任务中常见的相邻帧泄漏问题；再次，MiDaS 深度缓存机制显著降低了正式训练时的重复推理开销；随后，多任务数据集设计保证了图像、深度和检测标注在统一空

## 07_chapter_06.tex

- 未含引用段落数：`26`

1. 行号约 `5`；分类：待人工判断
   片段：在完成数据流构建之后，下一步核心问题是：如何把车辆检测、雾类型识别和雾浓度回归整合到同一网络中，并在保证训练可行性的同时兼顾后续部署需求。相比单任务模型，统一多任务模型既要考虑共享特征的提取方式，也要考虑检测头类别适配、分类头和回归头结构设计、损失函数组织以及训练稳定性控制。本章将围绕这些问题展开，重点说明本文统一多任务模型的结构设计与训练优化思路。
2. 行号约 `9`；分类：待人工判断
   片段：本文统一多任务模型定义在 \code{src/model/unified_model.py} 中，命名为 \code{UnifiedMultiTaskModel}。模型整体结构采用“YOLO 共享主干 + 两个附加头”的形式，即：
3. 行号约 `29`；分类：待人工判断
   片段：本文场景主要关注道路车辆，而不需要保留通用目标检测中的 80 类全部语义。因此，系统在配置中明确设定 \code{NUM_DET_CLASSES = 1}，类别名称为 \code{["vehicle"]}。然而，基础权重 \texttt{yolo11n.pt} 原本是在多类别数据上训练得到的，其检测头输出维度与单类任务不一致。为解决这一问题，本文在模型初始化
4. 行号约 `35`；分类：待人工判断
   片段：雾分类头的输入来自 YOLO 高层共享特征，而非直接使用原图像。其结构为：
5. 行号约 `43`；分类：待人工判断
   片段：之所以采用全局平均池化，是因为雾类型识别更关注整幅画面的整体退化模式，而不是某个局部区域的细节。通过池化操作，模型可将空间特征压缩为全局描述向量，再通过全连接层完成三分类。当前雾类别定义为：
6. 行号约 `51`；分类：待人工判断
   片段：这三类设置兼顾了业务可理解性与训练可实现性。\texttt{clear} 表示无雾，\texttt{uniform} 表示均匀低能见度，\texttt{patchy} 则用于表示局地非均匀团雾。在实际管理中，\texttt{patchy} 比 \texttt{uniform} 更值得关注，因为它更容易诱发突发性风险。
7. 行号约 `55`；分类：待人工判断
   片段：回归头同样基于共享高层特征设计，其结构为：
8. 行号约 `63`；分类：待人工判断
   片段：其中最终使用 \texttt{Sigmoid} 将输出压缩到 \texttt{[0,1]} 范围，再在训练与推理阶段按 \texttt{Config.BETA\_MAX} 做缩放。这意味着模型回归的并不是原始物理量纲下的绝对散射系数，而是相对于预设上界的归一化值。当前实现中，回归目标在 clear 样本上被显式置为 0，在有雾样本上由在线造雾过程给出真实采样
9. 行号约 `65`；分类：待人工判断
   片段：beta 回归的意义并不只是提供一个数字。对于系统应用而言，连续 beta 值可以进一步用于显示平滑、动态阈值控制、预警等级划分和小地图可视化，因此它是连接模型输出与业务联动的重要桥梁。
10. 行号约 `69`；分类：待人工判断
   片段：与许多简单封装直接调用 \code{self.yolo(x)} 的方式不同，本文在 \code{forward()} 中手动遍历 \code{self.yolo.model} 各层前向传播，并尝试捕获 SPPF 或检测头前最后一张高层 4D 特征图作为共享特征。这一实现看似复杂，但非常必要。因为若只调用高层封装接口，往往难以在推理结构变化和训练/验证模式差异
11. 行号约 `71`；分类：待人工判断
   片段：本文采用的共享特征提取策略是：优先捕获 \texttt{SPPF} 层输出；若当前 YOLO 架构中无法明确识别 \texttt{SPPF}，则退化为检测头前最后一张 4D 特征图。这种设计提升了模型对不同 YOLO 规模和结构变体的适应性，也避免了把通道数写死在代码里。
12. 行号约 `75`；分类：待人工判断
   片段：训练阶段，检测、分类和回归三项任务同时纳入优化目标。具体而言：
13. 行号约 `89`；分类：待人工判断
   片段：虽然当前实现中三项任务权重均为 1.0，但这并不意味着三项损失在数值尺度上天然相等。实际训练中，检测损失往往明显高于雾分类与回归损失，这也是当前日志中“det 占主导，fog\_cls 与 fog\_reg 数值更小”的原因。对此，本文采取两种应对方式：一是通过共享高层特征让天气任务充分利用检测主干已有的表达能力，减轻天气头从零学习的压力；二是在实验设计中预
14. 行号约 `93`；分类：待人工判断
   片段：相比只关注最终精度的论文实现，本文训练脚本在工程稳定性方面做了较多保护设计。
15. 行号约 `95`；分类：待人工判断
   片段：首先，训练过程支持自动续训。系统会在 checkpoint 目录中查找最新权重，若存在则恢复模型、优化器、调度器和 AMP scaler 状态，并从下一个 epoch 继续训练。这对于长时间训练、多次中断和实验迭代都十分重要。
16. 行号约 `97`；分类：待人工判断
   片段：其次，训练过程支持缺失深度缓存自动补算。训练开始前会统计训练集和验证集各自缺少多少深度文件，若有缺失则自动调用预计算流程补齐，避免训练到验证阶段才因文件缺失报错。
17. 行号约 `99`；分类：待人工判断
   片段：再次，脚本支持 AMP 混合精度训练，并监控非有限梯度比例。当前代码中实现了对 \texttt{NaN/Inf} 梯度的检测、告警、阈值判定以及必要时自动关闭 AMP 的恢复策略。这种设计在大 batch、长时间训练或量化感知训练切换中尤为重要。
18. 行号约 `101`；分类：待人工判断
   片段：最后，系统保留了 QAT 与 INT8 转换路径。虽然当前毕业设计阶段更关注功能打通与方法验证，但在 \texttt{src/train.py} 中已经实现了 QAT 准备、observer 修复和 INT8 模型转换流程，为后续边缘部署提供了明确方向。
19. 行号约 `105`；分类：待人工判断
   片段：随着项目从“链路打通”进入“结果收口”阶段，当前工作区中的训练产物已经不再只有单一默认模型目录，而是形成了多条具有明确职责的权重谱系。
20. 行号约 `107`；分类：待人工判断
   片段：第一条是默认统一模型谱系，对应 \code{outputs/Fog\_Detection\_Project}。它主要承担早期主链验证功能，证明统一模型可以完成数据装载、在线造雾、多任务前向、反向传播和权重保存等基本流程。第二条是偏雾天适配谱系，对应 \code{outputs/Fog\_Detection\_Project\_fogfocus}。该路线通过提
21. 行号约 `109`；分类：待人工判断
   片段：为避免“论文里说一套、工程里跑一套”，当前项目还把这些权重进一步收口到 \code{configs/benchmark\_models.json} 中，形成 \texttt{benchmark\_v1} 的四类 baseline 角色：
22. 行号约 `118`；分类：待人工判断
   片段：这种做法的重要意义在于：训练脚本、benchmark 脚本和论文实验章节可以围绕同一组可追踪模型目录展开，而不是依赖人工记忆某个临时权重文件名称。
23. 行号约 `120`；分类：待人工判断
   片段：在 \code{fogfocus\_full} 正式训练完成后，项目又针对统一模型在 clear-weather control clips 上检测能力偏弱的问题，继续分化出三条“检测能力回拉”实验分支：
24. 行号约 `128`；分类：待人工判断
   片段：从训练框架角度看，这三条分支说明本文脚本体系已经具备较强的实验组织能力：一方面支持通过环境变量快速切换损失权重、冻结策略、恢复 checkpoint 和检测头模式；另一方面也支持 \texttt{model-only} 恢复、\texttt{nonstrict} 参数加载以及重置 epoch 等结构迁移场景。换言之，当前训练代码不只是“能把一个模型训起来”，
25. 行号约 `130`；分类：待人工判断
   片段：需要强调的是，分支实验“被实现出来”并不自动意味着“实验结论为正”。恰恰相反，当前代码库最有价值的一点，是把多条尝试路径的成功与失败都沉淀为了可复查的脚本、日志和 benchmark 结果。后续第七章将进一步结合 \code{candidate\_benchmark\_summary.json}、\code{multi\_model\_benchmark\_
26. 行号约 `134`；分类：待人工判断
   片段：本章从模型结构和训练策略两个层面，系统说明了本文统一多任务方案的实现方式。首先，本文采用“YOLO 共享主干 + 雾分类头 + beta 回归头”的结构，把检测、分类和回归三类任务统一到同一网络中；其次，通过单类车辆检测头适配、共享高层特征捕获和联合损失组织，使统一模型既能保留预训练检测骨干的表达能力，又能兼顾雾天气任务的学习需求；再次，通过 checkpo

## 08_chapter_07.tex

- 未含引用段落数：`21`

1. 行号约 `5`；分类：系统设计/实现自述，可合理无引文
   片段：对于本科毕业设计而言，方法是否真正“成型”并不只看训练脚本能否运行，还要看模型是否能够稳定接入视频流、输出可解释结果、完成可视化展示，并为后续部署预留接口。因此，在统一多任务模型训练完成之后，还需要从系统实现角度把推理、后处理、可视化、混合推理、离线路线评估和导出部署组织成一条完整链路。本章将围绕这一目标，对当前项目的推理系统设计与部署实现方式进行说明。
2. 行号约 `9`；分类：系统设计/实现自述，可合理无引文
   片段：统一模型推理由 \code{src/inference.py} 中的 \code{HighwayFogSystem} 封装完成。系统首先根据配置自动解析模型权重路径，优先选择 \code{unified_model_best.pt}；若不存在，则回退至 \code{unified_model.pt} 或最新 checkpoint。随后，系统对视频帧执行如下流
3. 行号约 `23`；分类：系统设计/实现自述，可合理无引文
   片段：这种推理链保留了训练-推理一致性，也使雾估计不再只是附加文本输出，而是直接参与检测显示策略控制。
4. 行号约 `29`；分类：系统设计/实现自述，可合理无引文
   片段：该模块首先基于 IoU 对跨帧检测框进行轻量级关联，形成轨迹；随后为每条轨迹维护 \texttt{tentative}、\texttt{confirmed}、\texttt{suspicious} 和 \texttt{suppressed} 四类状态，并统计以下特征：
5. 行号约 `41`；分类：系统设计/实现自述，可合理无引文
   片段：其中，时序过滤的核心思想不是“静止即删除”，而是：只有当某轨迹在连续多帧内同时满足“位移极小、尺度变化极小、局部运动极低，并且置信度或外观复核概率不足以证明其为真实车辆”时，才将其从 \texttt{confirmed} 转为 \texttt{suspicious} 或 \texttt{suppressed}。这样做可以尽量避免把真实堵车、缓行或临停车辆错误
6. 行号约 `43`；分类：系统设计/实现自述，可合理无引文
   片段：在此基础上，本文还实现了 patch 级二次外观复核器。默认情况下，该复核器可以使用轻量 MobileNetV3\_Small 的 ImageNet 预训练语义作为车辆先验；若工作区中存在项目内训练得到的二分类权重，则同一接口还可切换为 \texttt{vehicle / non\_vehicle} 二分类模式。由于该复核器只运行在少量可疑静止轨迹上，而不是
7. 行号约 `47`；分类：系统设计/实现自述，可合理无引文
   片段：在低能见度场景下，固定检测阈值容易造成漏检。若仍使用与晴天相同的较高阈值，模型可能在轻度或中度雾中丢失一些本就较弱的车辆响应。为此，本文在推理阶段设置基础阈值 \code{BASE_CONF_THRES = 0.25}，再根据平滑后的 beta 值进行动态调整。当前代码中的核心思想可以概括为：
8. 行号约 `49`；分类：系统设计/实现自述，可合理无引文
   片段：\[ conf_{adaptive} = \max(conf_{min}, conf_{base} - k\hat{\beta}) \]
9. 行号约 `53`；分类：系统设计/实现自述，可合理无引文
   片段：其中 \texttt{conf\_min = 0.15}，\texttt{k = 1.2}。这意味着当雾浓度上升时，系统会适当降低显示阈值，保留更多潜在目标。虽然这种策略还不是严格意义上的风险最优控制，但它体现了一种重要思想：天气感知结果应当反馈到目标检测显示策略，而不是与检测分离。
10. 行号约 `57`；分类：系统设计/实现自述，可合理无引文
   片段：若运行设备为 CUDA，\texttt{HighwayFogSystem} 会创建预处理流、推理流和后处理流，尽量让不同阶段形成轻量级流水线。虽然在当前项目规模下，多流并不能使每一步都完全并行，但它有助于降低 CPU/GPU 空转等待，使视频演示更加流畅。该设计说明本文在实现中不仅考虑了算法正确性，也兼顾了运行效率与展示效果。
11. 行号约 `61`；分类：系统设计/实现自述，可合理无引文
   片段：在本文工程推进过程中发现：当前统一模型虽然在雾分类与 beta 估计上已形成较稳定的输出，但对真实下载雾视频中的车辆检测效果仍不如原始预训练 \texttt{yolo11n.pt} 稳定。考虑到毕业设计不仅要验证统一建模思路，还需要形成“车辆 + 雾状态 + 雾浓度”同时可视化且可连续演示的系统结果，本文进一步实现了位于 \texttt{scripts/hy
12. 行号约 `63`；分类：系统设计/实现自述，可合理无引文
   片段：该脚本采用如下策略：
13. 行号约 `73`；分类：系统设计/实现自述，可合理无引文
   片段：从严格学术角度看，混合推理不等于统一模型最终目标；但从工程验证角度看，它很好地解决了“系统需要立刻可用，而统一模型尚在持续优化中”的现实问题。其意义在于保证整个毕业设计在方法研究与系统演示之间形成平衡。
14. 行号约 `84`；分类：系统设计/实现自述，可合理无引文
   片段：需要指出的是，当前统一模型输出的是整帧一个 beta 值，而不是逐像素的雾浓度图。因此，右下角的小地图并不是模型直接回归得到的“真值级空间雾图”，而是一种基于图像局部对比度、饱和度和亮度白化程度估算得到的相对空间雾浓度分布图，并使用全局 beta 对其进行强度校正。
15. 行号约 `86`；分类：系统设计/实现自述，可合理无引文
   片段：这一设计虽然不是严格物理反演，但具备三个优点：第一，能够在演示视频中直观体现“同一画面不同位置雾强度可能不同”；第二，结合全局 beta 可获得相对稳定的整体浓淡变化；第三，工程实现简单，不需要额外训练密集预测网络。在当前项目阶段，它已经足以承担“工程解释增强层”的职责，并为后续是否值得引入真正的密集雾浓度预测头提供观察依据。
16. 行号约 `101`；分类：系统设计/实现自述，可合理无引文
   片段：随着训练分支逐步增多，仅靠人工观看单个演示视频已无法稳定回答“哪一版权重更适合作为论文正式结果”“统一路线是否真的优于混合路线”“某次检测器修补试验是否值得继续”这类问题。为此，项目围绕 \code{scripts/evaluate\_inference\_routes.py}、\code{scripts/run\_multi\_model\_benchma
17. 行号约 `103`；分类：系统设计/实现自述，可合理无引文
   片段：其中，\code{scripts/evaluate\_inference\_routes.py} 的职责是：在同一采样策略下，对统一路线和混合路线同时跑离线评估，输出逐帧 CSV、轨迹日志、逐帧过滤摘要、差异代表帧和总报告。它不伪造无法从视频直接得到的标注型 mAP，而是统计每帧检测数、平均置信度、fog 概率、beta 时序稳定性以及静止误检启发式指标。这
18. 行号约 `105`；分类：系统设计/实现自述，可合理无引文
   片段：\code{scripts/run\_multi\_model\_benchmark.py} 则在单模型路线评估之上增加一层编排能力，使多组雾权重能够共享同一 benchmark 视频集和同一采样策略，并输出跨模型的 \code{model\_overview.csv}、\code{model\_video\_matrix.csv} 与综合 \code{mu
19. 行号约 `107`；分类：系统设计/实现自述，可合理无引文
   片段：进一步地，\code{scripts/benchmark\_candidate\_model.py} 又把“候选模型是否能替代当前 baseline”变成了一套门槛判断流程。它会把候选模型临时并入 baseline 集合，然后根据统一路线平均检测数、非零检测帧占比、fog 切换率和 beta 抖动等指标，与各 baseline 逐一比较并给出 gate 结果
20. 行号约 `109`；分类：系统设计/实现自述，可合理无引文
   片段：最后，\code{scripts/postprocess\_fogfocus\_full.py} 把“正式训练完成后做什么”也标准化了：它会自动找到最新训练 run，对最佳权重执行路线评估，并进一步触发候选模型对标，从而输出适合论文和答辩使用的精简总结。换言之，当前项目中的推理与部署并不只是“模型能跑起来”，而是已经具备“训练结束后自动沉淀可写入论文的结构化
21. 行号约 `113`；分类：系统设计/实现自述，可合理无引文
   片段：本章围绕推理实现、可视化展示和部署准备三个层面，对本文系统的运行时链路进行了说明。首先，统一模型推理流程保证了训练与推理几何约束的一致性，并通过 beta 平滑和动态阈值联动增强了结果可解释性；其次，轨迹级时序过滤与二次外观复核使系统具备了视频级误检治理能力；再次，混合推理方案解决了当前阶段统一模型检测分支尚未完全成熟时的工程可交付问题；最后，离线路线评估、

## 09_chapter_08.tex

- 未含引用段落数：`57`

1. 行号约 `5`；分类：结果解读/结论展望，可合理无引文
   片段：在完成系统总体设计、数据构建、模型训练以及推理部署实现之后，论文需要进一步回答两个问题：第一，当前方法在训练和验证层面取得了怎样的定量结果；第二，当前工作区中的不同权重分支、不同推理路线以及误检治理链在工程上究竟表现如何。本章将结合正式训练摘要、formal benchmark、UA-DETRAC 标准检测指标、典型误差案例和静止误检治理产物，对本文系统的实
2. 行号约 `9`；分类：结果解读/结论展望，可合理无引文
   片段：本文实验并非只为了给出某一个孤立精度数值，而是围绕以下五个目标展开：
3. 行号约 `19`；分类：结果解读/结论展望，可合理无引文
   片段：因此，本文实验评价体系不再局限于单一训练损失或单个视频截图，而是同时覆盖训练曲线、离线路线评估、多模型 benchmark、候选模型 gate 结果以及静止误检治理产物。
4. 行号约 `23`；分类：结果解读/结论展望，可合理无引文
   片段：当前项目运行环境如下：
5. 行号约 `36`；分类：结果解读/结论展望，可合理无引文
   片段：统一模型与推理系统的关键运行参数包括：
6. 行号约 `48`；分类：结果解读/结论展望，可合理无引文
   片段：从工程角度看，这样的配置具有两个优点：其一，能够在 12GB 级别显存设备上完成训练、验证与视频推理；其二，QAT、ONNX 与 TensorRT 路径均已保留，后续向更轻量设备迁移时不必重写主流程。
7. 行号约 `54`；分类：结果解读/结论展望，可合理无引文
   片段：基于当前工作区的真实运行结果，训练与验证数据规模如表~\ref{tab:dataset_split_stats} 所示。
8. 行号约 `71`；分类：结果解读/结论展望，可合理无引文
   片段：与此同时，当前原始训练数据目录共包含 60 个监控序列，测试目录包含 40 个序列；深度缓存文件数量已经与原始训练图像数量对齐，说明 \code{outputs/Depth\_Cache} 中的 \code{.npy} 文件覆盖完整。对本文而言，这一点非常重要，因为它意味着论文中的训练结论不是建立在“少量手工挑样本”的临时环境上，而是建立在已经整理完毕的真实
9. 行号约 `97`；分类：结果解读/结论展望，可合理无引文
   片段：与只使用单段真实雾视频相比，引入多段 clear-weather control clips 的意义在于：它把统一模型的主要短板暴露得更加明确。若一个权重只能在单段雾视频上“看起来还可以”，却在多段晴天交通对照视频上持续丢失检测能力，那么该权重并不适合作为最终工程路线的检测主模型。
10. 行号约 `101`；分类：结果解读/结论展望，可合理无引文
   片段：当前项目的评价指标分为两层。
11. 行号约 `105`；分类：结果解读/结论展望，可合理无引文
   片段：第一层是训练过程中的学习指标，包括：
12. 行号约 `116`；分类：结果解读/结论展望，可合理无引文
   片段：这些指标主要服务于训练稳定性判断，回答“模型是否在收敛”“是否数值稳定”“某种损失权重策略是否可训练”等问题。
13. 行号约 `120`；分类：结果解读/结论展望，可合理无引文
   片段：第二层是离线路线评估中的视频级工程指标。与常见目标检测论文不同，当前阶段的真实雾视频并不具备逐框人工标注，因此本文不伪造形式上的 mAP，而是使用更贴近当前业务目标的指标：
14. 行号约 `133`；分类：结果解读/结论展望，可合理无引文
   片段：这些指标与当前代码实现严格对应，尤其对应 \code{scripts/evaluate\_inference\_routes.py}、\code{scripts/run\_multi\_model\_benchmark.py} 和 \code{scripts/benchmark\_candidate\_model.py} 中的结构化输出。因此，论文中的实验结
15. 行号约 `139`；分类：结果解读/结论展望，可合理无引文
   片段：当前论文正式采用的主训练结果来自 \code{outputs/Fog\_Detection\_Project\_fogfocus\_full}。这轮训练从 \code{fogfocus} 路线已有 checkpoint 继续，核心意图是：在关闭检测损失、冻结检测器的条件下，更集中地优化雾分类头与 beta 回归头。
16. 行号约 `141`；分类：结果解读/结论展望，可合理无引文
   片段：本轮训练的关键设置如表~\ref{tab:fogfocus_full_setup} 所示。
17. 行号约 `166`；分类：结果解读/结论展望，可合理无引文
   片段：训练结果摘要如表~\ref{tab:fogfocus_full_result} 所示。
18. 行号约 `189`；分类：结果解读/结论展望，可合理无引文
   片段：该结果表明，当前系统已经不再停留在“仅能完成一次 smoke 级试跑”的阶段，而是能够在完整 epoch 条件下稳定推进雾任务优化。尤其是 \texttt{best\_loss = 1.0072} 的出现，使 \code{outputs/Fog\_Detection\_Project\_fogfocus\_full/unified\_model\_best.
19. 行号约 `214`；分类：结果解读/结论展望，可合理无引文
   片段：为了观察正式雾权重在真实雾视频上的表现，本文首先对根目录中的 \code{gettyimages-1353950094-640\_adpp.mp4} 做单视频路线评估。该视频分辨率为 768$\times$432，帧率为 30 FPS，在 \texttt{sample\_stride = 5} 下共抽样 168 帧。
20. 行号约 `216`；分类：结果解读/结论展望，可合理无引文
   片段：根据 \code{scripts/postprocess\_fogfocus\_full.py} 触发的路线评估结果，\texttt{fogfocus\_full} 在该视频上的关键表现如表~\ref{tab:getty_real_fog_result} 所示。
21. 行号约 `238`；分类：结果解读/结论展望，可合理无引文
   片段：这一结果首先说明：\texttt{fogfocus\_full} 已经明显打破默认统一模型在真实雾视频上“几乎全判 \texttt{clear}”的塌缩现象。换言之，雾分支的正式训练确实起到了作用。其次，它也同时说明：雾分支的改善并没有自动转化为检测分支的根本性提升。即使统一路线已经能稳定输出非 \texttt{clear} 主导的天气判断，其车辆检测表现仍
22. 行号约 `251`；分类：结果解读/结论展望，可合理无引文
   片段：仅看单段真实雾视频仍不足以支撑“论文正式模型选择”。为此，项目进一步使用 \texttt{benchmark\_v1} 对 4 套 baseline 模型做了 formal 多模型 benchmark。参与对比的模型分别是：
23. 行号约 `260`；分类：结果解读/结论展望，可合理无引文
   片段：聚合结果如表~\ref{tab:benchmark_v1_aggregate} 所示。
24. 行号约 `281`；分类：结果解读/结论展望，可合理无引文
   片段：从中可以得到三点直接结论。
25. 行号约 `283`；分类：结果解读/结论展望，可合理无引文
   片段：第一，\texttt{fogfocus} 与 \texttt{fogfocus\_full} 在统一路线检测数和非零检测帧占比上并列当前最强，说明“偏雾天气头强化”确实能带来比默认统一模型更好的综合视频级表现。第二，\texttt{videoadapt} 虽然在单段目标视频适配上具有工程意义，但从当前 \texttt{benchmark\_v1} 的 5 视
26. 行号约 `285`；分类：结果解读/结论展望，可合理无引文
   片段：值得注意的是，\texttt{fogfocus\_full} 虽然与 \texttt{fogfocus} 在统一路线检测指标上并列，但它的 \texttt{weighted\_beta\_mean} 更低、\texttt{weighted\_beta\_abs\_delta\_mean} 略好，而 \texttt{weighted\_dominant\_sw
27. 行号约 `289`；分类：结果解读/结论展望，可合理无引文
   片段：进一步按视频展开 \texttt{fogfocus\_full} 的结果，可得到表~\ref{tab:fogfocus_full_per_video}。
28. 行号约 `309`；分类：结果解读/结论展望，可合理无引文
   片段：这张表把统一模型当前的真正短板暴露得非常清楚：在真实 fog 视频上，统一路线和混合路线之间已经存在稳定差距；而在 4 段 clear-weather control clips 上，这种差距进一步被放大。由此可见，当前统一模型的问题并不只是“雾头不够稳定”，更关键的是 \textbf{clear-weather traffic detection capa
29. 行号约 `313`；分类：结果解读/结论展望，可合理无引文
   片段：为了补足更标准的检测侧证据，本文新增实现了 \code{scripts/evaluate\_uadetrac\_detection\_metrics.py}，在 UA-DETRAC 验证集上对若干关键权重做单类车辆检测评估。该脚本沿用论文正文一致的输入尺寸、letterbox 几何约定和验证划分规则，在清晰天气域上统计 Precision、Recall、mA
30. 行号约 `332`；分类：结果解读/结论展望，可合理无引文
   片段：为使表~\ref{tab:uadetrac_standard_det_metrics} 的数值结果更具可读性，图~\ref{fig:detection_error_cases} 进一步给出了 \texttt{fogfocus\_full} 权重在 UA-DETRAC 验证集上的一例典型漏检和一例典型误检。左图中，高亮框对应被漏检的车辆目标；右图中，高亮框对应
31. 行号约 `341`；分类：结果解读/结论展望，可合理无引文
   片段：表~\ref{tab:uadetrac_standard_det_metrics} 至少说明了三件事。
32. 行号约 `343`；分类：结果解读/结论展望，可合理无引文
   片段：第一，若只看 UA-DETRAC 清晰天气验证集上的标准检测指标，默认 unified 模型实际上是当前表现最好的。这与其训练来源是一致的：默认 unified 检测分支直接在当前单类车辆任务上优化，因此在同域 clear-weather 验证集上取得最高 Precision、Recall 和 mAP 并不意外。
33. 行号约 `345`；分类：结果解读/结论展望，可合理无引文
   片段：第二，\texttt{fogfocus} 与 \texttt{fogfocus\_full} 的检测指标完全一致，这与当前训练策略严格对应。原因在于 \texttt{fogfocus\_full} 正式训练阶段冻结了检测器并把 \code{DET\_LOSS\_WEIGHT} 设为 0，因此其正式训练主要改变的是雾分类头和 beta 回归头，而不是检测分支本
34. 行号约 `347`；分类：结果解读/结论展望，可合理无引文
   片段：第三，\texttt{yolo11n} 在 UA-DETRAC clear-weather 验证集上的标准检测指标反而低于经过单类任务适配的 unified 检测分支。这一点看似与前文的混合路线推荐结论相矛盾，但实则回答的是不同问题：UA-DETRAC 标准检测指标衡量的是“在同域清晰验证集上，检测器对单类车辆标注的拟合质量”；而 \texttt{bench
35. 行号约 `349`；分类：结果解读/结论展望，可合理无引文
   片段：进一步从论文写作角度看，这张标准检测表的重要意义在于，它让本文不再只是依赖视频级现象判断检测分支的好坏，而是补上了一张更符合目标检测研究习惯的量化表。与此同时，它也迫使我们对当前系统做出更细致的分层判断：\textbf{默认 unified 在同域 clear-weather 车辆检测上并不弱；当前真正困难的，是如何把这种清晰天气检测能力与真实雾场景下的天气
36. 行号约 `353`；分类：结果解读/结论展望，可合理无引文
   片段：为了尝试修复上述短板，项目在 \texttt{fogfocus\_full} 之后继续开展了三条检测能力回拉分支：\texttt{clearpreserve\_pilot}、\texttt{detectorrecovery\_pilot} 和 \texttt{cocohead\_pilot}。它们的定位并不相同：
37. 行号约 `361`；分类：结果解读/结论展望，可合理无引文
   片段：这三条分支在 formal candidate benchmark 中的聚合结果如表~\ref{tab:detector_recovery_branches} 所示。
38. 行号约 `381`；分类：结果解读/结论展望，可合理无引文
   片段：需要特别强调的是，表中的 \texttt{fail} 不是论文作者主观评价，而是由 \code{scripts/benchmark\_candidate\_model.py} 根据统一路线平均检测数、非零检测帧占比、fog 切换率和 beta 抖动等门槛自动给出的 gate 结论。三条分支全部未能通过 gate，说明当前问题并不能通过“临时补一轮检测损失”或
39. 行号约 `383`；分类：结果解读/结论展望，可合理无引文
   片段：进一步观察可以发现：
40. 行号约 `391`；分类：结果解读/结论展望，可合理无引文
   片段：从研究视角看，这些失败分支并非负担，而是很有价值的结果。它们说明：统一模型当前的检测短板并不是某一个超参数或某一个头部形状选择就能轻易修复，而更可能与“真实雾域和合成雾域的差距”“单类检测头长期冻结后的表示退化”“统一模型在 clear-weather control clips 上的检测表达不足”等更深层原因有关。也正因如此，论文最终应当保留这些失败结论，
41. 行号约 `397`；分类：结果解读/结论展望，可合理无引文
   片段：为把“静止背景物体持续被误检成车辆”的问题从经验现象收口成可评估对象，项目在 \code{Route\_Eval\_temporal\_smoke} 中加入了轨迹日志、逐帧过滤摘要和启发式静止误检指标。当前 5 视频 smoke 路线评估在 \texttt{sample\_stride = 10}、\texttt{max\_frames = 40} 下得到的
42. 行号约 `415`；分类：结果解读/结论展望，可合理无引文
   片段：这些值不是带逐轨迹人工真值的严格误检率，但它们已经能够稳定回答两个问题：第一，当前静止误检现象确实存在，而且在混合路线中更严重；第二，时序过滤并不是“删掉所有低置信框”的粗暴后处理，而是以轨迹级形式显式记录了确认延迟、静止候选数量和已抑制静止候选数量。
43. 行号约 `419`；分类：结果解读/结论展望，可合理无引文
   片段：在当前工作区中，时序过滤器已经能够把被判为静止可疑或已抑制的轨迹导出为 patch。当前一轮 smoke 运行共导出了 16 个 patch，其中统一路线 3 个、混合路线 13 个。随后，\code{scripts/build\_static\_fp\_review\_pack.py} 会基于置信度、运动强度、道路先验和外观概率，为这些 patch 赋予 
44. 行号约 `421`；分类：结果解读/结论展望，可合理无引文
   片段：在此基础上，项目又保守收口出一版 \code{Hard\_Negatives\_static\_fp\_v1}。当前该数据集中共有 4 个 bootstrap hard negatives，全部来自真实雾视频中的高置信非车辆误检。尽管规模很小，但这已经足以证明“轨迹日志导出 $\rightarrow$ patch 复核 $\rightarrow$ boots
45. 行号约 `425`；分类：结果解读/结论展望，可合理无引文
   片段：基于上述 bootstrap negatives 和从 UA-DETRAC 中裁切得到的车辆正样本，当前项目训练了一版 MobileNetV3\_Small 二分类复核器。其训练摘要显示：
46. 行号约 `433`；分类：结果解读/结论展望，可合理无引文
   片段：更重要的是，当前工作区并不是只有“分类器训练成功”的离线结果，而是已经把这版权重回接到了 \code{TemporalVehicleFilter} 中。一次面向真实雾视频的 second-stage smoke 路线评估显示，轨迹日志中已经明确出现 \texttt{appearance\_non\_vehicle} 作为抑制原因，且 \texttt{seco
47. 行号约 `435`；分类：结果解读/结论展望，可合理无引文
   片段：当然，就当前 1 段视频、40 个抽样帧的 second-stage smoke 结果而言，还不足以严谨宣称“二次复核器已经显著降低静止误检率”。原因在于当前视频规模很小、阈值设置仍较保守，而且 per-minute 指标在超短视频上会被放大。更准确的结论应当是：\textbf{二次复核器的训练、加载、打分和回接路径已经打通，但其泛化收益仍需在更大视频集上继
48. 行号约 `439`；分类：结果解读/结论展望，可合理无引文
   片段：综合 formal training、formal benchmark 和误检治理实验，可以得到以下判断。
49. 行号约 `441`；分类：结果解读/结论展望，可合理无引文
   片段：第一，当前项目最稳固的成功点在于雾分支。无论从 \texttt{fogfocus\_full} 的训练曲线、单段真实雾视频上的非 \texttt{clear} 主导结果，还是 5 视频 formal benchmark 中 \texttt{fogfocus / fogfocus\_full} 对默认基线的提升来看，雾分类与 beta 回归已经从“能跑”进入“
50. 行号约 `443`；分类：结果解读/结论展望，可合理无引文
   片段：第二，当前统一模型最明确的短板仍然是检测分支，尤其是在 clear-weather control clips 上的车辆检测能力。formal benchmark 与三条失败回拉分支都指向同一结论：这一短板不是某个临时微调脚本就能轻易修补的，因此当前最务实的系统路线仍然是“统一模型负责雾感知，独立 YOLO 负责车辆检测”的混合方案。
51. 行号约 `445`；分类：结果解读/结论展望，可合理无引文
   片段：第三，项目的工程价值已经不再只体现在“有一个模型文件”。当前代码库已经形成了配置统一、训练可恢复、benchmark 可复用、候选模型可 gate、静止误检可回灌的完整实验基础设施。对于毕业设计而言，这一点非常关键，因为它使论文不再只是展示一个静态结果，而是展示一个可持续迭代的工程研究框架。
52. 行号约 `449`；分类：结果解读/结论展望，可合理无引文
   片段：尽管论文主线已经能够和当前代码及实验产物保持一致，但从严格研究角度看，仍存在以下不足。
53. 行号约 `459`；分类：结果解读/结论展望，可合理无引文
   片段：因此，后续更严格的补充实验应优先围绕以下三类展开：其一，在 UA-DETRAC 验证集上补充标准检测指标；其二，在更多真实 fog 视频上扩展 \texttt{benchmark\_v2}；其三，对静止误检治理链做成体系的 ablation，而不是只保留 smoke 路线证据。
54. 行号约 `463`；分类：结果解读/结论展望，可合理无引文
   片段：若结合陕西交通运输部门当前的运行管理现状来看，本文项目的应用价值可以从“可接入、可解释、可联动、可渐进部署”四个维度进一步理解。
55. 行号约 `471`；分类：结果解读/结论展望，可合理无引文
   片段：最后，从“可渐进部署”角度看，当前 formal benchmark 的结论与陕西场景的工程现实较为一致。当前离线对比已经反复表明：统一模型在雾感知层面已经有明显进展，但在车辆检测层面仍弱于混合路线。因此，若面向陕西交通部门开展原型验证，最务实的选择并不是强行坚持“一个模型做完所有事”，而是优先采用“正式 fog 权重 + 独立 YOLO 检测”的混合方案。
56. 行号约 `473`；分类：结果解读/结论展望，可合理无引文
   片段：综合来看，本文项目与陕西交通部门现状的结合，不应停留在论文背景层面的“举例说明”，而应进一步明确为一种可落地的应用路径：以既有监控视频为输入，以雾风险识别为核心，以协同联动和分级处置为目标，在陕西现有监测预警和路网运行体系中形成一个可逐步接入、逐步验证、逐步扩展的智能感知模块。
57. 行号约 `477`；分类：结果解读/结论展望，可合理无引文
   片段：本章围绕训练结果、视频级 benchmark、标准检测指标、误差案例以及静止误检治理链，对当前工作区中的实验产物进行了系统分析。结果表明：一方面，\texttt{fogfocus\_full} 已经能够作为正式论文中的雾任务参考权重，说明统一模型的雾分类与 beta 回归分支具备较稳定的训练与推理表现；另一方面，formal benchmark 又明确暴露出

## 10_chapter_09.tex

- 未含引用段落数：`9`

1. 行号约 `5`；分类：结果解读/结论展望，可合理无引文
   片段：结合当前代码实现与工作区产物，本文已形成以下几项较为明确的结论。
2. 行号约 `16`；分类：结果解读/结论展望，可合理无引文
   片段：从研究贡献角度看，本文最重要的价值并不只是给出一版权重文件，而是给出了一种较为完整的问题处理框架。该框架在数据层面提供了“晴天监控视频复用 + 深度缓存 + 在线造雾”的监督构造策略，在模型层面提供了“检测 + 雾分类 + beta 回归”的统一多任务建模方式，在工程层面则提供了从训练、推理、benchmark 到误检治理与回灌的全链路原型。对于毕业设计而言
3. 行号约 `18`；分类：结果解读/结论展望，可合理无引文
   片段：当然，本文仍处于原型系统阶段，后续工作至少还可以从以下几个方向继续推进。
4. 行号约 `20`；分类：结果解读/结论展望，可合理无引文
   片段：首先，应继续扩充真实雾域验证集。目前 \texttt{benchmark\_v1} 已经从单视频模板扩展到 5 段 active 资产，但其中真实 fog 视频仍只有 1 段。下一步最重要的工作之一，是继续追加轻雾、中雾、局地团雾、不同光照和不同车流密度下的真实监控视频，形成更具代表性的 \texttt{benchmark\_v2}。
5. 行号约 `22`；分类：结果解读/结论展望，可合理无引文
   片段：其次，应补齐标准检测指标与更系统的消融实验。当前论文已经用视频级工程指标较清楚地展示了路线优劣，但在更严格的学术表达上，仍应在 UA-DETRAC 验证集上补充 Precision、Recall 和 mAP 等标准检测指标，并对“是否使用深度缓存”“是否使用 \texttt{patchy} 模式”“是否接入时序过滤 / 二次复核器”等因素进行系统性消融。
6. 行号约 `24`；分类：结果解读/结论展望，可合理无引文
   片段：再次，应提升 beta 的物理标定能力。当前 beta 已具备连续、稳定和可解释的相对意义，但尚未与路侧能见度仪、气象站或激光雷达观测建立严格映射关系。未来若能够获取这类外部标定数据，则可进一步把 beta 从“相对雾强度代理变量”推进为更接近真实能见度量化的业务指标。
7. 行号约 `26`；分类：结果解读/结论展望，可合理无引文
   片段：此外，在静止误检治理方面，还需继续扩大 bootstrap negatives 与人工复核规模。当前二次复核器已经证明了训练、加载、打分与回接流程可行，但若要在更复杂场景中具备稳定泛化能力，就必须引入更多真实误检 patch、多视频来源和更细致的人工标签。
8. 行号约 `28`；分类：结果解读/结论展望，可合理无引文
   片段：最后，在部署层面，本文已经预留了 QAT、INT8、ONNX、TensorRT 与 Jetson 路径。未来可以围绕边缘设备上的推理速度、功耗、时延和稳定性开展更系统测试，使该系统真正从“桌面端研究原型”走向“路侧可部署的小型智能模块”。
9. 行号约 `32`；分类：结果解读/结论展望，可合理无引文
   片段：总的来说，本文已经完成了一套从理论方法到工程系统的完整探索。虽然统一模型在车辆检测能力上仍未超越成熟独立检测器，且真实 fog 视频覆盖、标准检测指标和二次复核器规模仍有待进一步加强，但其方法框架、代码实现、formal benchmark 体系和误检治理闭环已经具备较强的继续扩展价值。对于毕业设计终稿而言，当前这版论文与代码、实验产物之间已经能够形成较稳定
